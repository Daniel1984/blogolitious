doctype 5
html
  head
    title= title
    link(rel='stylesheet', href="/stylesheets/#{stylesheet}.css")
    meta(name='viewport', content='width=device-width, initial-scale=1')
    script
      var blogolitious_mobile = #{device} //Global variables
      var blg_user = !{JSON.stringify(user)}
 
  body#site(data-spy='scroll')
    block content
  
    block scripts
